# Money Flow Backend

This is the backend for the Money Flow project, built with Spring Boot.

## Technologies

- Java
- Spring Boot
- Maven

## Getting Started

To run the project locally:

1. Clone the repository
2. Navigate to the project directory
3. Run `./mvnw spring-boot:run` (or `mvnw.cmd` on Windows)
